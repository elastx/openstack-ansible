---
logrotate_applications:
  - name: neutron
    definitions:
      - logs:
          - /var/log/neutron/*.log
        options:
          - copytruncate
          - daily
          - missingok
          - rotate 14
          - compress
          - dateext
          - maxage 30
          - notifempty
          - nocreate
