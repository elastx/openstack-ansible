---
logrotate_applications:
  - name: nova
    definitions:
      - logs:
          - /var/log/nova/*.log
        options:
          - copytruncate
          - daily
          - missingok
          - rotate 14
          - compress
          - dateext
          - maxage 30
          - notifempty
          - nocreate
