---
logrotate_applications:
  - name: nova
    definitions:
      - logs:
          - /var/log/nova/*.log
        options:
          - daily
          - rotate 52
          - missingok
          - notifempty
          - compress
          - delaycompress
