---
logrotate_applications:
  - name: apache2
    definitions:
      - logs:
          - /var/log/apache2/*.log
        options:
          - copytruncate
          - daily
          - missingok
          - rotate 14
          - compress
          - dateext
          - maxage 30
          - notifempty
