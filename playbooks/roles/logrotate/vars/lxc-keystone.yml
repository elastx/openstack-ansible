---
logrotate_applications:
  - name: apache2
    definitions:
      - logs:
          - /var/log/apache2/*.log
        options:
          - daily
          - rotate 52
          - missingok
          - notifempty
          - compress
          - delaycompress
