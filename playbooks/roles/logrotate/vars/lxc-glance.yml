---
logrotate_applications:
  - name: glance
    definitions:
      - logs:
          - /var/log/glance/*log
        options:
          - copytruncate
          - daily
          - missingok
          - rotate 14
          - compress
          - dateext
          - maxage 30
          - notifempty
