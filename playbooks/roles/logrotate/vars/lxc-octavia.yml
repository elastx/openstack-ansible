---
logrotate_applications:
  - name: octavia
    definitions:
      - logs:
          - /var/log/octavia/*.log
        options:
          - copytruncate
          - daily
          - missingok
          - rotate 14
          - compress
          - dateext
          - maxage 30
          - notifempty
