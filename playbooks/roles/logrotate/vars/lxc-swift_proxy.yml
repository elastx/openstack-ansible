---
logrotate_applications:
  - name: swift
    definitions:
      - logs:
          - /var/log/swift/*.log
        options:
          - copytruncate
          - daily
          - missingok
          - rotate 14
          - compress
          - dateext
          - maxage 30
          - notifempty
          - nocreate
